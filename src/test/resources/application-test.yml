spring:
  application:
    name: order-service
  cloud:
    loadbalancer:
      enabled: true
    discovery:
      enabled: true
      client:
        simple:
          instances:
            user-service[0]:
              uri: http://localhost:8081

eureka:
  client:
    enabled: false